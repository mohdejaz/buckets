# Never bake secrets or local state into the image
.env
.envrc
buckets.db
buckets.db-journal

venv/
__pycache__/
*.pyc

.git/
.gitignore
.github/
.claude/
README.md
docs/
run.sh
run.bat
docker-compose.yml

# The QuickTime original of the walkthrough. media/buckets-intro-small.mp4 is
# the one that ships; keep the full-size source out of the image — COPY . .
# would otherwise bake it in whether or not git tracks it.
Buckets.mov
